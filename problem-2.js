// for loop 
let dailyWork = ["1. Unlock The Module at -8pm", "2. Instant partice the videos", "3. Collects the notes properly", "4. after complete watthe full modules partice the modules", " Join the support seccion if u have any problem"];

for (i = 0; i < dailyWork.length; i++) {
    let printLayout = dailyWork[i];
    console.log(printLayout);

}

// while loop 
let dailyWorks = ["1. Unlock The Module at -8pm", "2. Instant partice the videos", "3. Collects the notes properly", "4. after complete watthe full modules partice the modules", " Join the support seccion if u have any problem"];
let z = 0;

while (z < dailyWorks.length) {
    let val = dailyWorks[z];
    z++;
    console.log(val)
}